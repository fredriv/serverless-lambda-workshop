service: hello

provider:
  name: aws
  region: us-east-1
  runtime: python3.7

functions:
  hello:
    handler: handler.hello

plugins:
  - serverless-localstack

custom:
  localstack:
    stages:
      - dev
